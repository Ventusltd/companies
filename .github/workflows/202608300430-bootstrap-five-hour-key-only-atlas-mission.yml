name: 202608300430 Retired Companies bootstrap wrapper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Superseded by the direct five-hour key-only Atlas watchdog.'
