name: 202608300232 Retired row-level Companies Atlas URL build

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'Retired: Companies relationships remain key-only.'
          echo 'Use one verified dataset-level Atlas URL template; do not duplicate URLs into Parquet rows.'
